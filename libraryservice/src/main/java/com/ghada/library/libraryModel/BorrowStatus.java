package com.ghada.library.libraryModel;

public enum BorrowStatus {
 BORROWED,RETURNED, LATE, VERY_LATE
}
// BorrowStatus is an enum that represents the different statuses of a borrowed item in the library. It includes the following values: